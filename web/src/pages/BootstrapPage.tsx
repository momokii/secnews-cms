import { PageStub } from "../components/PageStub";

export function BootstrapPage() {
  return (
    <PageStub
      title="Bootstrap"
      description="First-run admin setup lands with the bootstrap wave (F2)."
    />
  );
}
