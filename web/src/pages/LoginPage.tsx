import { PageStub } from "../components/PageStub";

export function LoginPage() {
  return (
    <PageStub
      title="Login"
      description="Authentication form lands with the auth wave (F1)."
    />
  );
}
